#ifndef MALLOC_H_
#define MALLOC_H_
#include <stddef.h>
void *my_malloc(size_t size);
// size_t is like a long integer
// A void pointer is like an anonymous pointer since it can be casted to another type
#endif