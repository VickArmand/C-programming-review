#include <stdio.h>
int strstr(char * substr, char * str)
{
    int counter = 0;
    while (str[counter] != '\0')
    {
        if(substr[0] == str[counter])
        {
            
        }
        counter++;
    }
}