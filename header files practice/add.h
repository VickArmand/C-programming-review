#ifndef _ADD_H //this means that if this is not defined then define it. This avoids redefinition of libraries 
#define _ADD_H
#include <stdio.h>
int add (int, int); //function prototype
int minus(int, int);
#endif